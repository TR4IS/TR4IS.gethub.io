name = "reachflow-auth"
main = "index.js"
compatibility_date = "2024-09-01"

# Secrets — set these via CLI (never put real values here):
#   wrangler secret put GITHUB_CLIENT_SECRET
#   wrangler secret put REPO_TOKEN
#
# Plain var (not secret — client_id is public):
[vars]
GITHUB_CLIENT_ID = "Ov23likbVZBfG5a3rnEu"
